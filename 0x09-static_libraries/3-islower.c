#include "main.h"
#include <stdio.h>

/**
 * _islower: checks if a string is lowercase
 * @c: parameter
 * Return: 1 if true
 * 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
